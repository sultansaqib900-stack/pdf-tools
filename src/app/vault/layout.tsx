import type { Metadata } from "next";

export const metadata: Metadata = {"title": "Secure PDF Vault - Encrypted Document Storage | PDFTools", "description": "Store PDFs securely in your browser with AES-encrypted vault. Password-protected local document storage."};

export default function Layout({ children }: { children: React.ReactNode }) { return children; }
