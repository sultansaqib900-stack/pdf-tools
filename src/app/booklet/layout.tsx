import type { Metadata } from "next";

export const metadata: Metadata = {"title": "Create PDF Booklet - N-Up Printing & Booklet Layout | PDFTools Premium", "description": "Create saddle-stitch page order or scale PDF pages into 2×2 and 4×4 N-up layouts. Premium."};

export default function Layout({ children }: { children: React.ReactNode }) { return children; }
