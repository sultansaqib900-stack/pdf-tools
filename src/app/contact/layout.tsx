import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Contact PDFTools — Support, Feedback & Bug Reports",
  description: "Get in touch with the PDFTools team. Report a bug, request a feature, ask about Premium, or send feedback. We reply to every message.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
