import type { Metadata } from "next";

export const metadata: Metadata = {"title": "Split PDF by Bookmarks - Extract Chapters | PDFTools Premium", "description": "Split PDF documents into separate files based on bookmarks and outline structure. Extract chapters, sections, and parts automatically. Premium feature."};

export default function Layout({ children }: { children: React.ReactNode }) { return children; }
