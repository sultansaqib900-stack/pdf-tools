import type { Metadata } from "next";

export const metadata: Metadata = {"title": "PDF Metadata Sanitizer - Remove Hidden Data from PDF | PDFTools Premium", "description": "Strip hidden metadata, author info, creation dates, and embedded data from PDFs. Privacy cleaner. Premium."};

export default function Layout({ children }: { children: React.ReactNode }) { return children; }
