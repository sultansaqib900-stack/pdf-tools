import type { Metadata } from "next";

export const metadata: Metadata = {"title": "Visual Contract & PDF Diff 2.0 | PDFTools Premium", "description": "Compare two PDF files side by side or with an interactive split-screen slider. Highlight additions, deletions, and layout changes."};

export default function Layout({ children }: { children: React.ReactNode }) { return children; }
