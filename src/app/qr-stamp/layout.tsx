import type { Metadata } from "next";

export const metadata: Metadata = {"title": "Add QR Code to PDF - QR Code Stamping Tool | PDFTools", "description": "Add QR codes and barcodes to any PDF page. Choose position, size, and data. Free browser-based stamping tool."};

export default function Layout({ children }: { children: React.ReactNode }) { return children; }
