import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Add QR Code to PDF — Stamp QR Codes Free Online",
  description: "Stamp QR codes onto PDF pages linking to any URL. Control size, position and which pages. Free, private, browser-based tool.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
