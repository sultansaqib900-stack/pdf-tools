import type { Metadata } from "next";

export const metadata: Metadata = {"title": "Bulk Rename PDF Files - Auto-Rename by Metadata | PDFTools Premium", "description": "Package multiple PDFs with filenames based on title, author, page count, and original filename metadata. Premium."};

export default function Layout({ children }: { children: React.ReactNode }) { return children; }
