import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Terms of Service — PDFTools Usage Terms",
  description: "The terms governing your use of PDFTools, including acceptable use, Premium subscriptions, refunds, warranties and limitation of liability.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
