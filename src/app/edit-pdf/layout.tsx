import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Edit PDF Online Free — Text, Images & Pages | PDFTools",
  description: "Edit PDF files free in your browser. Add text, images and annotations, reorder pages and fix documents without uploading anything.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
