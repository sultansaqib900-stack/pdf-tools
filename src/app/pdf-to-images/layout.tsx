import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "PDF to Images Online Free — Extract PNG Images | PDFTools",
  description: "Convert PDF pages to high-quality PNG images online for free. Extract every page as a separate image. 100% free, no uploads, all in your browser.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
