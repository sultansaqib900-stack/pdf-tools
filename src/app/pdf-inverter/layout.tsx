import type { Metadata } from "next";

export const metadata: Metadata = {"title": "PDF Color Inverter - Dark Mode & Accessibility Converter | PDFTools", "description": "Convert PDF colors: invert to dark mode, convert to grayscale, or increase contrast for accessibility. Free."};

export default function Layout({ children }: { children: React.ReactNode }) { return children; }
